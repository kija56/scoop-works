# Fonts

**Display font** (headings, hero): **Schibsted Grotesk** — loaded via Google Fonts in `app/layout.tsx`. No local files needed.

This folder can hold local font files (e.g. Lil Grotesk, Struve) if you switch back to a local display font.
